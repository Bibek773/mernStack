/*
  Function Parameters
  Topic: passing values into a function.
*/

const showStudent = (firstName, lastName, rollNumber) => {
  console.log("Student:", firstName, lastName, "Roll:", rollNumber);
};

showStudent("Bibek", "Ghimire", 9);
showStudent("Bibek", "Ghimire", 10);
